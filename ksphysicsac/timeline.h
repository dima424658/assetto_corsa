#pragma once

class TimeLine
{
TimeLine *`vector deleting destructor'(unsigned int a2);
void TimeLine(const vec3f *p1, const vec3f *p2, int iid, TimeLineType type);
void ~TimeLine();
int check(const vec3f *p);

};